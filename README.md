# Credit Card Database Scan

First, make sure you have [Node js](https://nodejs.org/en/download/) installed.

In the root directory, install the packages

```bash
npm install
```

Then, build the project and start

```bash
npm run build
npm run start
```
